export const DOC_CHANGELOG = `# 更新日志

## 开发中

## 1.4.5 - 2026-07-20

- 同步工作台：左侧选择运行实例和主控窗口，右侧集中提供窗口、文本、标签页三类批量操作，并支持分组筛选。
- 页面操作同步：继续通过本机 CDP 同步点击、确认键、输入内容、滚动位置和页面跳转。
- 窗口管理：支持平铺、层叠、最大化、最小化和恢复所选浏览器窗口。
- 文本管理：支持向各窗口当前聚焦的输入框批量写入或清空文本，并显示逐窗口失败原因。
- 标签页管理：支持批量新建、跳转、刷新、关闭当前页，以及从主控窗口覆盖同步普通网页标签页。
- 发布版本：本次为窗口同步器优化，版本号保持 1.4.5；本地程序、Windows 安装包和便携包重新构建。

## 1.4.4 - 2026-07-19

- 书签开关：每条默认书签支持全局启用或关闭，关闭后不会写入新启动的实例。
- 单实例关闭：可以只对指定实例关闭某条书签，其他实例继续显示。
- 启动一致性：实例启动时同时过滤书签栏和“启动打开”网址。
- 安全同步：仅移除应用写入且已关闭的书签，不删除用户手工创建的同 URL 书签；运行中的实例停止后再次启动生效。
- 数据兼容：新增关闭状态的 SQLite 字段并兼容配置备份和旧数据库自动迁移。
- 发布版本：本地程序、Windows 安装包和便携包统一升级为 1.4.4。

## 1.4.2 - 2026-07-18

- 品牌图标：启用新的“联结 L”应用图标，以双向链路表达多环境连接与共享能力。
- 图标统一：同步更新 Windows 主程序、安装程序、系统托盘、Linux 应用图标、前端 Logo 和 favicon。
- 小尺寸适配：Windows ICO 提供 16 至 256 像素层级，提升标题栏、任务栏和快捷方式辨识度。
- 发布版本：本地程序、Windows 安装包和便携包统一升级为 1.4.2。

## 1.4.1 - 2026-07-18

- Chrome 数据导入：修复 Windows 下目录占用检测返回 \`exit status 1\` 的问题。
- 占用检测：收窄 CIM 进程查询并优化超时保护，降低高负载时的误报。
- 错误诊断：PowerShell 查询失败时展示实际错误原因。
- 发布版本：本地程序、Windows 安装包和便携包统一升级为 1.4.1。

## 1.4.0 - 2026-07-18

- 分组书签：默认书签支持分组、多级目录和组内排序，并写入 Chromium 真实文件夹。
- 书签导入：支持 Chrome、Edge、Firefox HTML 书签导入，保留目录并跳过重复 URL。
- 同步与备份：书签分组会随 SQLite、配置备份和已有未运行实例同步一起保存。
- 列表布局：实例列表和代理池按数据自动分配列宽，减少空白且不再逐字换行。
- 发布版本：本地程序、Windows 安装包和便携包统一升级为 1.4.0。

## 1.3.1 - 2026-07-17

- Chrome 数据导入：可直接选择 Chrome 的 \`--user-data-dir\` 完整目录并复制为独立实例。
- 导入安全：校验 Chrome 目录结构和占用状态，并跳过缓存、临时文件与锁文件。
- 错误提示：修复后端返回字符串错误时未展示 Chrome 目录占用等具体原因的问题。
- 指纹风险提醒：Chrome 导入实例的指纹实际发生变化时，保存前提示新设备识别、风控验证和重新登录风险。
- 界面精简：移除独立“教材”页面及顶部入口，说明集中到文档中心和 README。

## 1.3.0 - 2026-06-23

- 自动化增强：完善自动化脚本导入、运行、目标实例选择和执行记录管理。
- 插件管理：支持插件安装、导入、启停、删除、实例限制和单实例插件配置。
- VPN 优化：优化 Xray、sing-box、Mihomo 等连接栈的启动、测速、检测和预热能力。
- 实例迁移：支持实例导入导出，可将实例配置和完整浏览器用户数据目录打包迁移到新环境。
- 代理适配：实例导入时按代理名称匹配本地同名代理，匹配不到或同名不唯一时自动清空代理。
- 界面优化：优化实例列表、关键字展示、操作菜单和导入导出入口。

## 1.2.0 - 2026-05-09

- 接口调用：Launch API 补齐实例增删改查、按 code / selector 启动、runtime session / status / stop 和统一 CDP 入口。
- 自动化接口：脚本执行支持 selector / params 覆盖和 timeoutMs 超时控制，双实例 runtime 流程支持超时取消与错误返回。
- 代理池增强：新增链式代理导入、编辑和预览能力，支持 HTTP / SOCKS5 两层链路，并优化直连代理批量导入。
- 代理检测：新增测速目标、IP 健康检测目标和桥接启动超时配置，链式代理也可以参与测速与健康检测。
- 实例启动：代理异常时支持本次直连启动，不修改实例原有代理配置；默认代理池只保留直连节点。
- 书签能力：新增 IP 检测站点默认书签，支持设置启动时自动打开，并可同步到已有未运行实例。

## 1.1.0 - 2026-03-19

- Linux 支持：补齐 Linux 环境下的开发、打包、安装、启动与运行链路，并修复安装版启动与退出稳定性问题。
- macOS unsigned 内测：支持原生 macOS 主机打包 app / zip，用户状态目录迁移到 Application Support。
- SOCKS 代理测试：SOCKS 代理能力进入测试阶段，继续验证稳定性与兼容性。
- 接口触发浏览器：实验性支持通过接口启动浏览器实例，为自动化流程接入做准备。

## 1.0.0

- 实例隔离管理：支持创建、编辑、启动、停止、重启、克隆和删除浏览器实例。
- 代理池配置：支持统一维护代理节点，将代理绑定到具体实例，并导入 Clash 配置。
- 内核管理：支持维护多个 Chrome 内核，设置默认内核，并在实例中选择使用。
- 快捷启动：支持通过实例 Code 和快捷入口打开目标实例。
- 标签与检索：支持按标签、关键字、状态、代理、内核、分组筛选实例。
- 本地化存储：配置和实例数据保存在本地，适合长期使用和备份。
`
